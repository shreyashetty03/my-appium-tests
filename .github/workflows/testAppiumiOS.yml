name: Boot iOS Simulator and Launch App

on:
    push:
#     branches: 
#         - 'testBranch'
    workflow_dispatch:

jobs:
  ios-simulator-launch:
    runs-on: macos-latest

    env:
      DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer

    steps:
      - name: Show available runtimes
        run: xcrun simctl list runtimes

      - name: Create and boot iOS simulator
        run: |
          # Get the latest available iOS runtime
          RUNTIME=$(xcrun simctl list runtimes | grep -m 1 "iOS 17" | grep -o 'com.apple.CoreSimulator.SimRuntime.iOS[^ ]*')

          # Define simulator type (e.g., iPhone 14)
          DEVICE_TYPE="com.apple.CoreSimulator.SimDeviceType.iPhone-15"

          # Create simulator and capture its ID
          SIM_ID=$(xcrun simctl create "CI-iPhone14" "$DEVICE_TYPE" "$RUNTIME")
          echo "SIMULATOR_ID=$SIM_ID" >> $GITHUB_ENV

          # Boot the simulator
          xcrun simctl boot "$SIM_ID"

          # Wait until boot completes
          xcrun simctl bootstatus "$SIM_ID" -b

      - name: Launch Safari on the simulator
        run: |
          echo "Launching Safari on simulator: $SIMULATOR_ID"
          xcrun simctl launch "$SIMULATOR_ID" com.apple.mobilesafari

      - name: Capture Screenshot
        run: |
            mkdir -p screenshots
            xcrun simctl io "$SIMULATOR_ID" screenshot screenshots/safari.png
            ls screenshots    

      - name: Upload screenshot
        uses: actions/upload-artifact@v3
        with:
            name: simulator-screenshot
            path: screenshots/safari.png      

      - name: List running simulators
        run: xcrun simctl list devices booted




# name: Run iOS Script on Emulator

# on:
#   push:
#     branches: 
#         - 'testBranch'
#   workflow_dispatch:

# jobs:
#   run-on-emulator:
#     runs-on: macos-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18' # or the version you need

#       - name: Install dependencies
#         run: npm install

#       - name: List available simulators
#         run: xcrun simctl list devices

#       - name: Boot iOS Simulator
#         run: |
#             xcrun simctl list devices
#             SIMULATOR_ID=$(xcrun simctl list devices | \
#             grep -m 1 "iPhone 15 Pro" | \
#             grep -oE '[A-F0-9\-]{36}')
#             echo "Booting simulator with UDID: $SIMULATOR_ID"
#             xcrun simctl boot "$SIMULATOR_ID"
#             xcrun simctl bootstatus "$SIMULATOR_ID" -b

#       - name: Run JS Script
#         run: |
#           node test3.js